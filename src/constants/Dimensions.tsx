import {Dimensions} from 'react-native';

export const maxHeight = Dimensions.get('screen').height;
export const maxWidth = Dimensions.get('screen').width;

export const modalHeight = maxHeight * 0.3; //30%
export const modalWidth = maxWidth * 0.8; //80%

export const width50 = maxWidth / 2;
export const width40 = maxWidth * 0.4;
export const width10 = maxWidth * 0.1;
export const width05 = maxWidth * 0.05;
